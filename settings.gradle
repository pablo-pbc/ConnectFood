pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        // Maskara reference https://github.com/santalu/maskara
        maven { url 'https://jitpack.io' } // Adicione esta linha
    }
}

rootProject.name = "ConnectFood"
include ':app'
